
string  policy_type   # can be used as lookup into zoo registry

string  policy_ver    # optional version (if not specified, defaults to "latest")

string  params_json   # optional JSON string containing config parameters

