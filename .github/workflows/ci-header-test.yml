name: SMARTS CI Header Test

on: [push]

env:
  venv_dir: .venv
jobs:
  test-formatting:
    runs-on: ubuntu-18.04
    container: huaweinoah/smarts:v0.4.3-pre
    steps:
      - name: header test
      run: |
        make header-test
